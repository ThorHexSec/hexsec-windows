---
# HexSec Windows — Ansible is a thin adapter around install.ps1
# Run from a control node that can reach the Windows host via WinRM,
# or use ansible.windows / community.windows with a local connection.

collections:
  - name: ansible.windows
    version: ">=2.0.0"
  - name: community.windows
    version: ">=2.0.0"
